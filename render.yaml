services:
  - type: web
    name: factugo
    runtime: node
    plan: free
    buildCommand: |
      cd client && npm install --include=dev && node node_modules/vite/bin/vite.js build
      cd ../server && npm install && node node_modules/prisma/build/index.js generate
    startCommand: |
      cd server && node node_modules/prisma/build/index.js db push --skip-generate && node node_modules/tsx/dist/cli.mjs src/index.ts
    envVars:
      - key: DATABASE_URL
        value: "file:./prod.db"
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/health
