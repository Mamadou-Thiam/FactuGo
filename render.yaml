services:
  - type: web
    name: factugo
    runtime: node
    plan: free
    buildCommand: |
      cd client && npm install --include=dev && npm run build
      cd ../server && npm install --include=dev && node node_modules/prisma/build/index.js generate && npx tsc
    startCommand: |
      cd server && node node_modules/prisma/build/index.js db push --skip-generate && node dist/index.js
    envVars:
      - key: DATABASE_URL
        value: "file:./prod.db"
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/health
