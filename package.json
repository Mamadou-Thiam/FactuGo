{
  "name": "factugo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "setup": "cd server && npx prisma generate && npx prisma db push && npm run dev"
  }
}
